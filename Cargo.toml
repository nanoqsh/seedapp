[package]
name = "seedapp"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.5"
tokio = { version = "1.20", features = ["rt-multi-thread"] }
tower = "0.4"
tower-http = { version = "0.3", features = ["fs"] }
